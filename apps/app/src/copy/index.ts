/** Barrel for the copy + behavior-config layer (card CENTRAL-1). */
export { COPY, type Copy } from "./strings";
export {
  CONFIG_DEFAULTS, useBehaviorConfig, confBand, type BehaviorConfig, type ConfBand,
} from "./config";
