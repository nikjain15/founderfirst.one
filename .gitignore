.DS_Store
*.swp
*~

# Secrets — never commit. (.env.example templates are allowed.)
.env
*.env
!*.env.example
.secrets/
secrets.env
*.key
*.pem

# Cloudflare CLI cache
.wrangler/

# Stress test output logs
test-results/

# Claude Code settings
.claude/

# Internal — not for the public repo
BookKeeping/
FounderFirst OS/
_dev/
CLAUDE.md
ANALYTICS.md

# Workspace — Phase 0+
node_modules/
.env.local
.env.*.local
apps/*/dist/
apps/*/.vitepress/dist/
apps/*/.vitepress/cache/
packages/*/dist/
# Final assembled deploy artifact — produced by `pnpm build`.
/dist/

# Database backups / dumps — contain real emails + auth rows, never commit.
_backups/
supabase/.temp/

# local build artifacts (worktree-only, never committed)
deno.lock
site-bubble/worker/pnpm-lock.yaml
site-bubble/worker/pnpm-workspace.yaml
scratchpad/
