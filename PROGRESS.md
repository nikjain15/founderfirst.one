# admin-hardening — PROGRESS

Cross-run memory for the autonomous overnight hardening of `apps/admin`.
**Read this first every run. Resume where you left off. Never commit a red build.**

Branch: `admin-hardening` (worktree). Base: `main`.
Status legend: `todo` · `wip` · `done` · `deferred` (see "Needs human review").
Verify column records HOW it was checked (tsc/build/harness@widths/CSS-review). Authed pages cannot be runtime-clicked — note that explicitly.

## Setup checklist (each run)
- [ ] Read CLAUDE.md, LEARNINGS.md, apps/admin/RESPONSIVE.md, packages/design-system/tokens.css
- [ ] `git worktree` is `admin-hardening`; on branch; up to date
- [ ] Read this matrix; pick next `todo`/`wip`

## Dimensions (priority order)
1. Responsiveness · 2. Design/Brand · 3. Security · 4. Architecture · 5. Code quality & scalability

## Route × Dimension matrix

| Route (file) | Resp | Design | Sec | Arch | Quality | Verified how |
|---|---|---|---|---|---|---|
| login (Login.tsx) | done | todo | todo | todo | todo | CSS review (login.css): card `max-width:440px;width:100%`, `.admin-main` pads ≥24px so card never kisses edges; `.field input`=16px. No fix needed. Authed n/a (unauthed but needs Supabase to render). |
| support / inbox (Inbox.tsx) | done | todo | todo | todo | todo | CSS review (inbox.css): `.ticket-row` grid-areas stack @≤480, `.toolbar`/`.ticket-meta` flex-wrap, `.kpi-strip` flex-wrap. Fluid, token-driven. CSS review only — authed, not runtime-clicked. |
| ticket detail (TicketDetail.tsx) | done | todo | todo | todo | todo | CSS review (ticket.css): `.ticket-header`/`.ticket-tags`/`.reply-actions` flex-wrap, `h1` clamp, `.msg` 32px+1fr (icon col). Fluid. CSS review only — authed. |
| audience (AudienceHome.tsx) | done | todo | todo | todo | done | Resp: CSS review — `.tabs` scroll-x, `.tab` `min-height:var(--tap-min)`, `.tab-panel` no fixed widths; sub-tabs render their own (already-verified) routes. Quality: completed ARIA tab pattern (id+aria-controls→tabpanel, aria-labelledby). tsc+build green. Authed → not runtime-clicked. |
| audience: users (Users.tsx) | done | todo | todo | todo | wip | drawer close-btn 44px; drawer-list grid now responsive (stacks @≤480, render harness @320/640 — no overflow, value gets full width); drawer has `role=dialog`+`aria-labelledby`. Clickable-row keyboard gap + shared-Drawer focus-trap logged (deferred). |
| audience: signals (Signals.tsx) | done | todo | todo | todo | wip | drawer close-btn 44px (shared CSS); drawer-list grid responsive (shared); drawer `role=dialog`+`aria-labelledby`. Escape-to-close NOT added — would discard unsaved lead edits (deferred). |
| audience: discord (DiscordLinks.tsx) | done | todo | todo | todo | todo | Resp: CSS review — `.toolbar` flex-wrap, search `.field` `flex:1 1 260px`, table in `.table-wrap` (data-table cells ellipsis + max-width:320px), `.row-actions` nowrap. Fluid, token-driven. Authed → not runtime-clicked. |
| analytics home (AnalyticsHome.tsx) | done | todo | todo | todo | todo | CSS review (analytics.css): `.kpi-grid`/`.mix-grid` auto-fit minmax, `.palette-row` stacks @≤640, `.feedback-*` flex-wrap, `.chart-wrap svg` width:100%. Fluid. CSS review only — authed. |
| analytics: marketing (AnalyticsMarketing.tsx) | done | todo | todo | todo | todo | shares analytics.css (auto-fit grids). CSS review only — authed. |
| analytics: product (AnalyticsProduct.tsx) | done | todo | todo | todo | todo | shares analytics.css + `.funnel-row` (misc.css) stacks @≤640. CSS review only — authed. |
| analytics: signals (AnalyticsSignals.tsx) | done | todo | todo | todo | todo | shares analytics.css. CSS review only — authed. |
| analytics: waitlist (AnalyticsWaitlist.tsx) | done | todo | todo | todo | todo | shares analytics.css + `.sparkline` (misc.css) width:100%. CSS review only — authed. |
| content home (ContentHome.tsx) | done | todo | todo | todo | todo | CSS review (content.css): `.tabs` scroll-x, `.subnav` flex-wrap, `.analytics-two-col`/`.prompt-editor-grid` stack @≤760, voice tables stack @≤640. Fluid. CSS review only — authed. |
| content: prompt (ContentPrompt.tsx) | done | done | todo | todo | todo | notes-input font fix; runtime harness @320/375 (16px Inter, no overflow) |
| content: voice (ContentVoice.tsx) | done | todo | wip | todo | todo | Resp: CSS review (content.css) — `.voice-header`/`.voice-header-sub` flex-wrap, voice say-this tables stack @≤640, chips wrap. Sec: marked→dangerouslySetInnerHTML XSS surfaced (deferred, see below). |
| emails (EmailHub.tsx) | done | todo | todo | todo | done | Quality(run 4): same ARIA tab completion as the other containers (tab id+aria-controls→`emails-tabpanel`, role=tabpanel+aria-labelledby, type=button). Resp: CSS review (emails.css): `.email-cols` two-col→one @≤860, `.email-brand-grid`/`.email-cadence`/`.email-stats` auto-fit minmax, all rows flex-wrap, sticky preview unsticks @≤860, inputs 16px. Fluid. CSS review only — authed. |
| how-it-works (HowItWorks.tsx) | done | todo | todo | todo | todo | CSS review (docs.css): `.docs-toc` flex-wrap, `.docs-feature` stacks @≤640, `.docs` max-width 72ch. Fluid. CSS review only — authed. |
| what's new (WhatsNew.tsx) | done | todo | done | todo | todo | Resp: CSS review (docs.css) — `.whatsnew-*` rows flex-wrap, composer inputs 16px, preview-frame width:100%. Sec: iframe srcDoc sandbox=""; tsc+build. CSS review only — authed. |
| quality (Quality.tsx) | done | todo | todo | todo | todo | CSS review (quality.css): `.quality-grid` `minmax(min(100%,220px),1fr)`, `.quality-headline`/`.quality-sev` flex-wrap, sparkline width:100%. Fluid (gold-standard). CSS review only — authed. |
| audit (Audit.tsx) | done | todo | todo | todo | wip | drawer close-btn 44px; drawer-list grid responsive (shared); drawer `role=dialog`+`aria-labelledby`. Clickable-row keyboard gap logged (deferred). |
| admins (Admins.tsx) | done | done | todo | todo | todo | Resp: CSS review (misc.css) — `.admins-invite` flex-end + stacks @≤640 (btn full-width), invite `.field` `flex:1 1 260px`, table in `.table-wrap`, `.btn` `min-height:var(--tap-min)`. Design: classes fully token-driven (`--fs-*`, `--r-pill`, `--error*`, `--ink*`). Authed → not runtime-clicked. Note: `.link-danger` Remove btn ~30px tall (dense-table convention) — see deferred. |

## Change log (append-only; newest first)
<!-- YYYY-MM-DD HH:MM · route · dimension · what changed · commit sha · verify -->
- 2026-06-24 (run 4) · emails (EmailHub) · Quality(a11y) · EmailHub's Templates/Scheduled/Activity bar uses the **same** `tablist`/`tab` markup as the other three (an earlier deferred note wrongly said it was a different non-ARIA pattern — corrected). Applied the identical fix: tab `id`+`aria-controls="emails-tabpanel"`, panel `role=tabpanel`+`aria-labelledby`, `type=button`. · ddd31b1 · tsc+build green; markup-only (no visual delta). Authed → not runtime-clicked.
- 2026-06-24 (run 4) · audience/analytics/content tab containers · Quality(a11y) · the three hash-backed tab bars declared `role=tablist`+`role=tab`+`aria-selected` but tabs had no `aria-controls` and the panel was a bare `<div>` → AT announced a tab with no linked tabpanel. Added `id={`tab-${id}`}` + `aria-controls="<route>-tabpanel"` to each tab and `role=tabpanel`+`id`+`aria-labelledby={`tab-${tab}`}` to the panel (stable panel id, so no dangling ref to the unrendered inactive panels). Also added missing `type="button"` to Analytics/Content tab buttons (Audience already had it). Pure markup — no class/style/layout/behavior change. Did NOT add roving-tabindex + arrow-key nav (the remaining WAI-ARIA tab gap) — see deferred. · e69b36b · tsc+build green; CSS-review (no visual delta possible — markup-only). Authed → not runtime-clicked.
- 2026-06-24 (run 4) · audience/discord/admins · Resp · audited the last three `todo` routes. All structurally clean: tables in `.table-wrap`, toolbars `flex-wrap`, invite/search `.field` `flex:1 1 260px`, `.admins-invite` stacks @≤640, `.tabs` scroll-x with `.tab` `min-height:var(--tap-min)`. No fix needed → Resp=done. Admins classes also fully token-driven → Design=done. · n/a (read-only) · CSS review (misc.css/inbox.css/tables.css/content.css)
- 2026-06-24 (run 3) · users/signals/audit drawers · Resp(a11y) · added `role="dialog"` + `aria-labelledby` (h2 `id="drawer-title"`) to all three detail drawers so screen readers announce a named dialog (was a bare `<aside>`). Pure ARIA — no layout/focus/behavior change; one drawer mounts at a time so the shared id is DOM-unique. Did NOT add `aria-modal`/Escape/focus-trap — see deferred "shared `<Drawer>`" item. · f44181f · tsc+build green
- 2026-06-24 (run 3) · users/signals/audit drawers · Resp · `.drawer-list > div` used fixed `grid-template-columns: 140px 1fr` (RESPONSIVE.md rule 2). On a full-width drawer @320 the value column was ~120px, cramping long emails/URLs/ids. → label col now `minmax(88px,140px)` (caps at 140 on wide, unchanged) and stacks to 1 column @≤480. Shared CSS → fixes all three drawers. · 41c8917 · tsc+build green; render harness w/ built CSS — @320 row stacks (value=271px full width, `documentElement.scrollWidth==innerWidth`, close btn 44×44, screenshot), @640 two-col `140px 279px` (wide preserved).
- 2026-06-24 (run 3) · login/inbox/ticket/analytics(×5)/content/emails/how-it-works/whatsnew/quality · Resp · completed the CSS responsive sweep over all 12 admin stylesheets. Every file is fluid-first + token-driven: grids use `auto-fit`/`minmax`/`min()`, rows `flex-wrap`, breakpoints (@480/@640/@760/@860/@1024) only change shape, inputs ≥16px, tables in `.table-wrap`. **The drawer-list grid was the only genuine rule-2 offender** — everything else already compliant. Marked these routes Resp=done via CSS review (authed → not runtime-clicked; logged per row). · n/a (read-only) · CSS review of all `apps/admin/src/styles/*.css`
- 2026-06-24 (run 2) · users/signals/audit drawers · Resp(a11y) · `.drawer-head` close button was a 16px SVG icon in a borderless button with no box → ~16px tap target (RESPONSIVE.md rule 4 violation). Replaced the dead `font-size:18px` (vestige of old text "×") with the standard 44×44 inline-flex icon-button box (matches `.nav-toggle`); negative margins keep the icon optically aligned to the drawer edge so the header doesn't grow. Shared CSS class → fixes all three drawers at once. · 233f379 · tsc+build green; render harness with **built** CSS @320: close btn computed = 44×44, `documentElement.scrollWidth==innerWidth` (no overflow), screenshot confirms × aligned to edge + value column wraps long URL.
- 2026-06-24 · cross-cutting · Design/Sec · audit sweep over all routes+CSS: **zero** hardcoded hex in CSS or TSX, every `<table>` is inside `.table-wrap`, every `target="_blank"` carries `rel="noreferrer"`, all `.field` inputs already ≥16px, global `:focus-visible` ring (base.css:29). Codebase token discipline is strong; few real defects. · n/a (read-only) · grep audit
  - **Correction (run 2):** the prior "zero inline numeric font-sizes — every one uses a `--fs-*` token" claim is **inaccurate**. The admin CSS uses raw px font-sizes (13/12/15/16/18/22/24/28px…) as a de-facto convention; only a handful use `--fs-*` tokens, and the token scale (`--fs-h1…--fs-data-row`) is marketing-oriented with no matching `--fs-13`/`--fs-14` etc. **Do NOT mass-tokenize** these — most `--fs-*` tokens are `clamp()` and would change the rendered size (not behavior-preserving). Color tokenization is clean and complete; font-size tokenization is intentionally partial and should be left as-is unless a human re-scopes the token set.
- 2026-06-24 · what's new (WhatsNew.tsx) · Security · added `sandbox=""` to the digest-preview `<iframe srcDoc>` so script in admin-controlled email HTML can't execute in the admin origin; HTML/CSS/images still render, Send button is outside the frame. · 2678b61 · tsc+build green
- 2026-06-24 · content: prompt (ContentPrompt.tsx) · Resp+Design · gave `.prompt-editor input[type=text]` notes fields `font-family:inherit; font-size:16px` (were UA-default ~13px non-Inter → iOS auto-zoom + typo drift). Both textareas keep inline mono overrides. · 91da8af · tsc+build + render harness inspect: input = 16px Inter; width-ladder @320 docScrollW==innerWidth (no overflow), table scrolls inside `.table-wrap`; screenshot @375

## Needs human review (deferred / risky)
<!-- item · why skipped · file:line · suggested action -->
- **Tab bars lack roving-tabindex + arrow-key navigation** · [AnalyticsHome.tsx:36](apps/admin/src/routes/AnalyticsHome.tsx), [ContentHome.tsx:42](apps/admin/src/routes/ContentHome.tsx), [AudienceHome.tsx:44](apps/admin/src/routes/AudienceHome.tsx) · run 4 completed the static ARIA tab semantics (tab↔tabpanel wiring), but a full WAI-ARIA tablist also wants: only the active tab in the tab order (`tabIndex` 0 on selected, -1 on the rest) and Left/Right/Home/End arrow-key navigation moving focus between tabs. · NOT auto-done: it changes the keyboard tab-stop model (today every tab is a Tab stop) — a real interaction change that should be designed once in a shared `<Tabs>` primitive and click-tested, not bolted onto three copies. Today all tabs are still reachable via Tab; this is an enhancement, not a regression. Suggested: extract a shared `<Tabs>` with roving tabindex + arrow keys and adopt it in all four tab containers (Analytics/Content/Audience/EmailHub — all now share the same completed static-ARIA markup).
- **`.link-danger` row-action button is ~30px tall (< 44px tap target)** · [misc.css:108](apps/admin/src/styles/misc.css) — the "Remove" (Admins) / "Revoke" (Discord) links use `padding:6px 8px` + `--fs-data-row`, so the touch target is ~30px, under RESPONSIVE.md rule 4. · NOT auto-fixed: these live inside dense `.data-table` rows (cell padding `10px 14px`); forcing `min-height:var(--tap-min)` on the link would inflate every row's height and change the table's visual density across Admins + Discord — a design tradeoff, not a clear bug. The drawer close-button (an isolated control) was correctly bumped to 44px in run 2; dense in-table action links are a different case. Suggested: a human decides whether to accept the denser 44px rows or move destructive actions into the detail drawer / an overflow menu.
- **Extract a shared `<Drawer>` component with full modal a11y** · drawer shell is duplicated verbatim in [routes/Users.tsx:133](apps/admin/src/routes/Users.tsx), [routes/Signals.tsx:617](apps/admin/src/routes/Signals.tsx), [routes/Audit.tsx:120](apps/admin/src/routes/Audit.tsx) (learning #6: one concept, three copies → will drift). A shared `<Drawer title onClose>` would let `aria-modal="true"`, focus-trap, initial-focus + focus-restore, and Escape-to-close be added **once**. · NOT done autonomously: (a) it's a 4-file refactor on authed pages I can't click-test (only CSS is harness-verifiable); (b) **Escape-to-close is unsafe for the Signals lead drawer** — it holds unsaved edits (stage/contact/notes in local state) that would be silently discarded; it needs an unsaved-changes guard, a product decision. This run added the safe subset (`role="dialog"` + `aria-labelledby`) in-place. Suggested: build the shared component, add focus-trap + `aria-modal`, and gate Escape/overlay-close behind a dirty-state confirm in the Signals drawer.
- **Clickable data-table rows are not keyboard-operable** · [routes/Users.tsx:121](apps/admin/src/routes/Users.tsx), [routes/Audit.tsx:106](apps/admin/src/routes/Audit.tsx) · `<tr onClick={…} className="row-clickable">` opens the detail drawer on mouse click but has no `tabIndex`/`onKeyDown`, so keyboard users can't open it (WCAG 2.1.1). · NOT auto-fixed because every clean option has a real downside: `tabIndex={0}` on every row makes a 100s-of-rows waitlist table one giant tab-trap; `role="button"` on a `<tr>` breaks table row semantics for screen readers. The correct fix is a design choice (e.g. make the first cell a real `<button>`, or add a dedicated "view" action column) — needs a human. Mouse + the drawer's own focus ring are unaffected today.
- **`marked.parse` → `dangerouslySetInnerHTML` (stored XSS surface)** · [routes/ContentVoice.tsx:72,353](apps/admin/src/routes/ContentVoice.tsx) · `marked` v5+ does not sanitize; raw HTML/`<script>`/`<img onerror>` in the voice-guide markdown reaches the DOM. Blast radius is limited (content is authored by allow-listed admins → self/compromised-admin XSS, admin origin only), so it's low severity, but it is a real sink. · Not fixed autonomously: the right fix adds a DOMPurify pass (new dep + could strip legitimate voice-guide HTML the `upgradeRenderedHtml` step relies on) — needs a human to choose the sanitizer config and re-verify the rendered voice guide. Suggested: `marked.parse` → `DOMPurify.sanitize(...)` before `upgradeRenderedHtml`, allow-listing the tags the guide actually uses (blockquote/table/ul/strong/em/code/h2/h3/p/a).
- **`pnpm audit` — admin-reachable transitive advisories (dependency bumps, lockfile-wide)** · run `pnpm -C apps/admin audit --prod`:
  - `ws` <8.21.0 (1 high DoS + 1 moderate mem-disclosure) via `@supabase/supabase-js@2.105.1`. **Real-world risk is low in this app**: the admin SPA is a browser build and uses the native `WebSocket`, not the Node `ws` package (only bundled in Node targets). Remediation: bump `@supabase/supabase-js` to a release that pins `ws ≥8.21.0`.
  - `react-router` 6.30.3 → patched **6.30.4** (moderate: same-origin redirect with `//`-prefixed path). Trivial patch bump.
  - Not applied autonomously: both touch the shared pnpm-workspace lockfile (affects demo/marketing too) and routing/realtime can't be fully runtime-verified this run (authed pages). Suggested: `pnpm -C apps/admin update react-router-dom@^6.30.4 @supabase/supabase-js@latest`, then `pnpm -C apps/admin build` + smoke the app while logged in.
  - Note: the other 14 advisories are in `apps/demo` / `apps/marketing` (posthog-js → dompurify) — **out of admin scope**, untouched.
