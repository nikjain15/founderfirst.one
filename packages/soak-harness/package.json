{
  "name": "@ff/soak-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Load/soak test harness for the ledger post RPC + Plaid sync paths, plus an additive structured-observability helper for edge functions. RV2-E production-readiness slice.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./observability": "./src/observability.ts"
  },
  "scripts": {
    "test": "vitest run",
    "soak": "tsx src/soak.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
